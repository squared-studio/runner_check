name: CHECK REYAD # TODO

on:
  push:
    branches: [ "main" ]

jobs:
  CHECK:
  
    runs-on: REYAD # TODO

    steps:
    - uses: actions/checkout@v4

    - name: CHECK
      shell: bash
      run:
        bash dependency_check.sh REYAD # TODO
        
